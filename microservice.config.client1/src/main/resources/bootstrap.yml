spring:
  application:
    name: configclient1
  cloud:
    config:
      uri: http://localhost:8888
      #label: branch1
      fail-fast: true

server:
  port: 8100

management:
  security:
    enabled: false
